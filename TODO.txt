Hippo local roadmap

Positioning
- Hippo is a Postgres-native durable workflow orchestrator for teams building long-running TypeScript workflow and agent harnesses.
- The product bet is setup simplicity, local/staging/prod parity, and Postgres-native durability primitives: same-txn step commit, outbox, LISTEN/NOTIFY, operator history, and run-scoped KV.
- The README should target people who want orchestration under an agent harness without splitting "core workflows" and "agent workflows" into separate product concepts.
- Keep external comparison language out of the README. Use concrete Hippo capabilities and quickstart proof instead.

Resolved product decisions
- Postgres is the only supported backend. SQLite is out of scope.
- Workers are stateless. Durable execution state lives in Postgres only.
- Workflow definitions are code-defined and runs pin `definition_version`.
- Retries are configured per step.
- Callback correlation keys are first-class persisted records.
- Inline JSON payloads are acceptable for normal control-plane data; large blobs should be stored externally and referenced.

Next up (ordered, work top-down)
1. Query/operator surfaces                                         — richer run listing, search, and admin workflow
2. OTel spans + deeper observability                               — production diagnostics
3. Time-travel rewind/fork                                         — major operator unlock
4. Tier 1 SDK monorepo split + publish                             — local public entrypoints exist; publishable packaging still remains
5. Examples directory with realistic workflows                     — prove the primitives on real flows
6. CLI ergonomics                                                  — operator CLI parity with dashboard
7. Architecture cleanup                                            — split oversized store/route/test files to match repo rules
8. Test hardening                                                  — CLI command coverage, scaffold smoke, and worker-loop edge cases

Critical code debt
- [ ] Split oversized handwritten TypeScript files to satisfy `.codex/rules/clean-architecture.md`.
  Current offenders: `src/lib/workflow-store.ts`, `src/routes/workflows.ts`, `src/lib/workflow-engine.test.ts`, and `src/lib/workflow-store.pg.test.ts`.
  Target split: store concerns into focused modules, route registration vs handlers/views, and long integration tests into feature suites.
- [ ] Add scaffold smoke coverage that runs generated app checks against the real generated files.
  Cover: package scripts, env defaults, workflow import path, dashboard start command, and generated README commands.

Wave 0 — Baseline durability claims
- [x] Exponential backoff + jitter + max delay cap
  Extend RetryPolicy: { maxAttempts, initialBackoffMs, maxBackoffMs, backoffMultiplier, jitterMs }. Replace getRetryAvailableAt linear formula with exponential + capped + jittered. Default policy preset.
- [x] Saga / compensate primitive
  Per-task steps can now define `compensate`. On terminal failure or hard terminate, the engine walks completed steps in reverse, persists compensation attempts with `kind = 'compensate'`, retries them under their own policy, and escalates terminal compensation failure as `compensation_failed`.
- [x] Task queues + priority
  Runs and schedules now carry `task_queue` and `priority`. Workers register one or more queues via `HIPPO_TASK_QUEUES`, claim filters by queue, and runnable ordering prefers `priority DESC` before availability time.

Tier 1 — table stakes for DE
- [x] Signals: push events into a running run
  Durable signal table + route + engine consume-or-open-wait branch + atomic consumeSignalAndResumeWait closing the post-openWait race (commit 31f4f83).
  Optional follow-up: client-supplied dedup_key with unique (run_id, signal_name, dedup_key) for idempotent signal posts.
- [~] Queries: read live state without mutation
  Current state: run detail, attempts, events, active/failed/stuck operator queries exist, plus filtered `GET /v1/operators/runs`, lineage inspection via `GET /v1/operators/runs/:runId/lineage`, and single-run context projection.
  Open: workflow-owned projection contracts for clients that should not consume raw context keys, plus pagination/cursor semantics once operator lists get larger.
- [~] Retry policy per step
  Current state: max attempts, exponential backoff, jitter, max delay cap, and non-retryable error tags are implemented.
  Open: richer retry classification and policy presets.
- [~] Idempotency keys / activity IDs
  Current state: stable step-scoped idempotency keys exist in execution context, and client-supplied `Idempotency-Key` headers on POST /runs dedup duplicate starts durably in Postgres.
  Open: explicit external activity/outbox helper for exactly-once side effects.
- [x] Durable timers / sleep
- [x] Wait expiry: timeoutMs required on wait/signal steps, recovery expires overdue waits and fails the run
- [x] Cron schedules
  workflow_schedules table + startScheduleLoop (src/lib/scheduler.ts) using cron-parser + nextFireAt, atomic claim and dispatch.
- [~] Child workflows + cancel propagation
  Current state: engine has kind === "child" branch, parent_run_id linkage, getChildRun + wakeParentForChild store methods, hard cancel propagation parent→children via operator routes, and compensation during hard terminate.
  Open: child-failure handling beyond happy path, child timeout, fan-out (parallel children).
- [~] Cancellation + termination
  Current state: durable cancel route exists, cancelMode column on runs ('graceful' | 'hard'), cancelRunAtBoundary store method, hard termination finalizes the durable run state immediately and prevents in-flight step commits from landing afterward.
  Open: explicit in-process step abortion signal if step bodies need cooperative teardown.
- [x] Step heartbeats for long activities / lease extension
  ctx.heartbeat() updates workflow_runs.lease_expires_at and workflow_step_attempts.last_heartbeat_at under owner+lease guard.
- [~] Versioning
  Current state: runs pin workflow definition version and execution now resolves the exact stored version instead of looking workflows up by name only. New starts pick the latest registered version for a workflow name.
  Open: deploy-safe execution for retired code paths, operator visibility into missing registered versions, and explicit upgrade policy.

Tier 2 — Postgres-native edge
- [x] Same-transaction step commit for workflow state + user DB writes
  `transactional: true` on task step → engine runs body inside SAVEPOINT hippo_step_body within the step txn (executeTransactionalTask in workflow-store.ts).
- [x] `FOR UPDATE SKIP LOCKED` worker pull
- [x] `LISTEN` / `NOTIFY` wakeups
  src/lib/notifier.ts: NOTIFY on workflow_runnable channel after run state mutations; worker subscribes via listenForWakeups.
- [x] Outbox pattern helper
  workflow_outbox table + enqueueOutbox in transactional step ctx + startOutboxLoop drain with topic→handler map.
- [x] Partition workflow_step_attempts + workflow_events by hash(run_id)
  Why: hot fan-out and long-running workflows grow both tables fast; partitioning gives writes parallelism and lets archival drop partitions wholesale instead of row-deletes.
  Current state: both history tables are now hash-partitioned into 16 children by `run_id`, branch lineage stores `(branched_from_attempt_run_id, branched_from_attempt_id)` so the FK remains valid on the partitioned attempts table, and the Postgres integration suite asserts partition pruning via `EXPLAIN`.
  Follow-up: revisit partition count only if sustained write pressure justifies moving from 16 to 32.
- [~] Continue-as-new + archival (history bounding)
  Current state: task steps can now return `continueAsNew`, which atomically completes the current run, starts a fresh run of the same workflow/version, and links it with `continued_from_run_id`.
  Open: archival job that moves completed-run events older than N days to workflow_archive_events (or external sink) and deletes from hot tables; declarative partitioning above makes archival a partition-drop.

Tier 3 — differentiation
- [~] Mermaid live render of run state with current step highlighted
  Current state: dashboard renders Mermaid workflow definitions and run detail views with live SSE-backed updates.
  Open: stronger current-step visualization and richer execution overlays on the rendered graph.
- [~] Time-travel replay (snapshot rewind / fork — Path A)
  Current state: workflow_step_attempts now persist monotonic `step_seq` plus `context_before`, runs persist branching lineage, operator APIs can `rewind` or `fork` a terminal run from a prior forward attempt, and both the dashboard and operator API expose run lineage chains.
  Open: support for non-terminal rewinds with wait/child cleanup semantics, plus explicit archival/replay UX on top of the branching primitives.
- [x] Human-in-the-loop task with URL + expiry
- [x] Webhook callback waits with built-in HMAC verification
- [x] OpenTelemetry spans
  Current state: dashboard reads, operator queries, run detail/context reads, SSE setup, start/cancel/retry/rewind/fork, engine start/tick/resume/compensation, worker ticks, scheduler ticks/dispatch, recovery passes, outbox delivery, and major store operations emit nested spans through the shared tracer. Step spans now carry attempt number, retry count, task queue, priority, child run id, and wait correlation attributes, and Hippo can bootstrap OTLP exporters from env with checked-in Honeycomb docs and an example snippet.
- [~] Typed TypeScript workflows + validated step IO
  Current state: typed workflow definitions exist.
  Open: Zod-validated step inputs and outputs.

Tier 3.5 — Advanced orchestration primitives (lower priority than DX/Wave-0)
- [ ] Workflow Updates (synchronous query-with-mutation)
  Client sends a request that mutates state and waits for result. Hippo could implement this as a signal variant with response channel (insert request, worker processes, response row, client polls or LISTEN/NOTIFY).
- [ ] Search attributes
  Optional JSONB `search_attributes` column on workflow_runs + GIN index. Operator search by `attrs @> '{"customerId":"..."}'`.
- [ ] Side effects helper
  Only needed if Path B replay (deterministic generator workflows) lands. Today hippo's step model already records non-deterministic values via step output.
- [ ] Nexus / cross-namespace operations
  Skip until multi-tenancy lands. Out of scope for v1.
- [ ] Workflow signals with response (signal-with-result)
  Equivalent to Updates above; pick one shape.

Tier 3.7 — Agent-shaped generic primitives
- [ ] Step taxonomy + dashboard renderer plugin API
  API: workflow registration includes step kind tags. Dashboard accepts plugin bundles that register renderers per kind: `registerStepRenderer("agent", AgentPanel)`. Hippo ships generic renderer for unknown kinds.
- [ ] Retry-with-augmented-input pattern
  Generic problem: retry where each attempt should be informed by the previous attempt's output + failure reason. Today retries are stateless re-calls.
  API: `iterativeStep({ initial: { ... }, refine: (prevInput, prevOutput, error) => nextInput, attempt: async (ctx, input) => output, maxAttempts: 3 })`
- [ ] Operator action plugin API
  Generic problem: dashboard needs custom operator actions per step kind beyond cancel/retry/rewind. Hippo provides dispatch + audit log row in workflow_events with `kind = "operator.action"`.
- [ ] Compensation refinement
  Refine compensation for per-step-kind compensation strategies and idempotency hints.
- [ ] Run-scoped event query API
  Filter run events by kind / time range / step attempt. Powers dashboard and external monitoring.
- [ ] Usage ledger UI panel
  Cost rollups per run / workflow / day. Top consumers. Top resources. Pure SQL aggregation on top of the usage ledger.

Tier 4 — SDK + packaging (PROMOTED — DX is the pitch, not features)
- [ ] Tier 1 SDK: TS in-process SDK (ship current capability as a real package)
  Goal: someone can `npm install` hippo, define workflows in their own repo, run a worker + API server, without copy-pasting from this repo. No new runtime features — pure packaging, public API surface, and DX.
  Current state: local `hippo/sdk`, `hippo/core`, and `hippo/server` entrypoints resolve through the root `exports` map, `packages/core`, `packages/sdk`, and `packages/server` now exist as workspace package previews, and `npm run build` mirrors the compiled runtime into those package directories for local smoke tests.
  Scope:
    1. Split the repo into packages (monorepo, e.g. npm workspaces or pnpm):
       - @hippo/core           engine + store + types (current src/lib + src/types + src/queries + src/sql)
       - @hippo/sdk            workflow definition helpers, step builders, public re-exports
       - @hippo/server         Fastify routes + worker tick loop + recovery loop (current src/app + src/routes + src/index + src/lib/worker + src/lib/recovery)
       - @hippo/cli            thin bin: `hippo migrate`, `hippo render <workflow>`, `hippo worker`, `hippo server`
    2. Public API surface in @hippo/sdk:
       - defineWorkflow({ name, version, startAt, steps })
       - Step builders: task(), wait(), signal(), sleep(), end() — each returns a typed step def matching current union, with required fields enforced (timeoutMs on wait/signal, next on sleep, etc.).
       - Optional Zod integration: task({ input: z.object({...}), output: z.object({...}), run: ... }) — validates at runtime; defaults to passthrough when no schema given.
       - Re-export StepExecutionContext, TaskStepResult, RetryPolicy, etc.
    3. Public API surface in @hippo/core:
       - createWorkflowStore(db), createWorkflowEngine({ definitions, metrics, store }), LostLeaseError, types.
       - Keep SQL files shipped alongside, exported as raw strings so embedders can run migrations.
    4. Public API surface in @hippo/server:
       - createApp({ engine, store, metrics, auth }) returning a Fastify instance.
       - startWorker({ engine, intervalMs, leaseMs, workerId }) returning a stop handle.
       - startRecoveryLoop({ store, intervalMs }) returning a stop handle.
    5. CLI in @hippo/cli (commander or cac):
       - `hippo migrate --database-url=...`  runs db/migrations/*.sql
       - `hippo render <workflowName>`       static Mermaid via renderWorkflowAsMermaid
       - `hippo worker`                      bootstraps store + engine + worker loop from env (DATABASE_URL, HIPPO_API_TOKEN, HIPPO_CALLBACK_SECRET)
       - `hippo server`                      same as `worker` but also starts the Fastify API
    6. Stability + DX:
       - Add `exports` map to each package.json; ESM-only; type=module.
       - One README per package with a minimum quickstart (< 20 lines of user code starting a workflow).
       - Top-level repo README points at the packages; current bootstrap README content gets demoted into examples/.
       - examples/ folder with a runnable demo workflow consuming @hippo/sdk + @hippo/server.
       - changesets or release-please for versioned publishing. Start at 0.x.
    7. CI: typecheck + test + lint at the workspace level; package each on tag.
  Non-goals (keep this Tier 1 — defer to Tier 2 SDK):
    - Remote worker protocol over HTTP/gRPC. Workers stay in-process. Polyglot SDKs out of scope.
    - Workflow code sandboxing or determinism enforcement.
    - Hosted service. Self-host only.
  Acceptance:
    - User in a fresh repo runs: `npm i @hippo/sdk @hippo/server @hippo/core pg && npx hippo migrate && node demo.js`, starts a workflow via HTTP, sees Mermaid render, signal resumes a wait step. Zero references to hippo source paths.
    - typecheck + test + lint pass at workspace root.
    - All existing tests pass after the move with import paths updated.

- [ ] Tier 2 SDK: remote worker protocol (deferred decision)
  Enables polyglot workers (Go, Python). Requires task_queues + pending_step_invocations tables, HTTP poll/complete endpoints, lease over HTTP. Conflicts with same-txn step commit moat — do not commit until polyglot demand is concrete.

Tier 5 — Productized local/staging/prod DX
  Goal: 5 minutes from `git clone` to seeing a workflow execute. Same config working locally, staging, prod with no rewrites.
  Primary actions to actually earn the simplicity claim:
    1. Collapse the public mental model toward one primary authoring primitive.
       Hippo is still workflow/step/engine/store/app-shaped. Keep the power, but add a simpler front door so the default path feels closer to "write one durable task/workflow and run it" instead of assembling multiple concepts first.
    2. Make scaffolded apps the main path, not repo-first setup.
       The source-repo flow works, but the productized path should be `npx hippo init` into a fresh app with a working dashboard, example run, and docs that never require reading Hippo internals.
    3. Finish the package and publish story.
       Local package previews exist, but simplicity does not count until users can install Hippo cleanly into their own repo without workspace-only caveats.
    4. Make local dev boringly reliable.
       `init -> dev -> dashboard -> start run -> inspect state -> signal/wait` should work every time on a fresh machine with no manual recovery, broken migrations, or surprise runtime mismatches.
    5. Polish the operator UI until it feels product-grade.
       The built-in dashboard is a core simplicity feature, not a side tool. Mermaid interactions, live state, filters, lineage, and operator actions need to feel trustworthy enough that users do not drop to raw APIs immediately.
    6. Hide boot wiring from the happy path.
       Users should not need to think about engine/store/app/worker composition just to get started. Keep the lower-level surfaces for embedding, but bias docs and scaffolds toward the smallest working runtime entrypoint.
- [~] `npx hippo init` scaffold command
  Current state: scaffold command exists in-repo today via `npm run hippo:init -- <dir>` and generates a runnable starter with `docker-compose.yml`, `.env`, `src/workflows/example.ts`, scripts, and README. The global `npx hippo init` shape stays blocked on packaging/publishing.
  Creates: docker-compose.yml (Postgres + hippo container), .env with safe defaults, src/workflows/example.ts, package.json scripts, README. One command, working demo.
- [x] `hippo dev` — single command starts everything
  Spawns Postgres via docker-compose (if not running), runs migrations, starts hippo server + worker with hot-reload of workflows (tsx watch). Logs colored, structured.
- [~] Web dashboard
  Current state: embedded `/dashboard` route with active/failed run lists, workflow definition Mermaid views, run detail page, and live event tail.
  Critical open: add a Hatchet-style trace view on run detail with `Overview / Traces / Logs` ergonomics and a realtime hierarchical timeline so operators can inspect parent/child execution, step durations, overlaps, retries, waits, and the currently-running segment as a live-updating Gantt-style tree.
  Embedded route /dashboard. Server-rendered (Fastify + minimal React or vanilla HTMX) — no separate frontend service. Views:
    - Run list with filter by workflow / status / search attrs
    - Run detail: live Mermaid render with current step highlighted, event log timeline, attempt history with input/output diffs, context viewer
    - Workflow definitions list with static Mermaid
    - Live tail of events via SSE
    - Operator actions: cancel, retry, rewind, fork (when Time-travel ships)
  No auth in dev (loopback only). Token auth in prod (reuse HIPPO_API_TOKEN).
- [~] Environment story: local / staging / prod parity
  Current state: `HIPPO_ENV=dev|staging|prod` is parsed centrally and non-dev now enforces `HIPPO_API_TOKEN` plus `HIPPO_CALLBACK_SECRET` at startup. `.env.example`, `.env.staging.example`, and `.env.prod.example` now document the baseline matrix, and deploy notes live in `docs/deploy.md`.
  Same code, env vars switch behaviour. Document the matrix:
    - DATABASE_URL                  → which Postgres
    - HIPPO_ENV=dev|staging|prod    → log level, auth strictness, dashboard auth
    - HIPPO_API_TOKEN / SECRET      → required in non-dev
    - HIPPO_HMAC_SKEW_SECONDS       → callback clock skew tolerance
  Open: validate the env matrix from a freshly scaffolded app and keep deploy docs aligned as process-model changes land.
- [x] Realtime run status streaming (SSE)
  GET /v1/runs/:runId/stream → SSE of events as they happen. Use LISTEN/NOTIFY on workflow_events insert. Both dashboard and client SDK consume.
- [~] Hot-reload workflow registration in dev
  Current state: `startWorkflowDevReloader` watches workflow files in the source dev entrypoint, reloads changed definitions, and `replaceDefinitions` preserves pinned versions for in-flight runs while exposing the newest version for new starts.
  Open: wire the same reload behavior into packaged `hippo server` / `hippo worker` flows when `HIPPO_ENV=dev`.
- [ ] Examples directory with realistic workflows
  - Webhook ingestion + retry + dead-letter
  - Order fulfillment with saga compensation
  - Cron-triggered nightly report
  - Long-running approval (signal + wait + expiry)
  - Fan-out via child workflows (when child workflows ship)
- [x] Throughput benchmark
  Current state: `bench/throughput.ts` provisions an isolated Postgres database per worker-count scenario, measures claims/sec plus p50/p95/p99 end-to-end latency, and records a null-claim contention proxy while backlog remains. `docs/BENCHMARKS.md` captures the reproduction command, hardware, and measured worker-scale results.
- [x] Deployment recipes
  Current state: `Dockerfile`, `fly.toml`, `railway.json`, `render.yaml`, and `docs/deploy.md` now cover both single-process (`HIPPO_ROLE=all`) and split-role (`HIPPO_ROLE=serve|work`) deploy shapes.
- [~] README quickstart < 5 minutes
  Current state: top-level README now describes the truthful source-repo flow (`npm run hippo:init -- my-app`), dashboard entrypoint, and example run command.
  Open: switch the docs to `npx hippo init my-app` once the CLI is packaged and published.
- [~] CLI ergonomics
  Current state: `hippo migrate`, `hippo render`, `hippo serve`, `hippo work`, `hippo all`, `hippo runs ls`, `hippo runs show <id>`, `hippo runs cancel <id>`, `hippo workflows ls`, and `hippo workflows render <name>` exist, with `server` and `worker` retained as aliases.
  Open: JSON/scriptable output mode, terminate/retry/rewind/fork commands, schedule commands, and packaging validation for `npx hippo`.

Cross-cutting acceptance for Tier 5
  - New user goes from zero to a running workflow visible in dashboard in under 5 minutes on a fresh machine with only Docker installed.
  - Same workflow code deploys to staging and prod with only env var changes.
  - Dashboard works on mobile / over SSH tunnel (no client-side framework bloat).
